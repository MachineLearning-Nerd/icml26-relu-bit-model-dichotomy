# Branch and attribution audit

Former repository name: `icml26-repro-nMS1YTjHMH-relu-bit-model-dichotomy`

Target repository name: `icml26-relu-bit-model-dichotomy`

The repository currently exposes only `main`; the clone contains no former
`orx/*` branches. There is therefore no branch experiment lineage to rename.
The current `main` tip contains the five-claim contract, pinned source, exact
quadratic SLP toy, raw evidence, and the documentation in the root.

## Publication checklist

- [x] README documents the paper, all five claims, repository contents, Claim 1 evidence path, citation, and thank-you note.
- [x] Current branch state is explicitly documented as `main` only.
- [x] No former `orx/*` branch names are present in the cloned refs.
- [x] Normalize all reachable commit author and committer identities.
- [x] Rename the GitHub repository.
- [x] Push the clean name and verify the live default branch, README, refs, and identity set.

## Attribution policy

All reachable commits will be normalized to:

```text
MachineLearning-Nerd <MachineLearning-Nerd@users.noreply.github.com>
```
